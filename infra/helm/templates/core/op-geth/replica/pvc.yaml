apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.geth.name }}-replica-pvc-datadir
  labels:
    app: {{ .Values.geth.name }}-replica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi # 1Tb recommended 
  storageClassName: gp2