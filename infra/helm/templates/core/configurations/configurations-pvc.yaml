apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: configurations-pvc
  labels:
    app: configurations
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
  storageClassName: gp2
