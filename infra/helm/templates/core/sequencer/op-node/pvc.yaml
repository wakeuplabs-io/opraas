apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.node.name }}-sequencer-pvc-bin
  labels:
    app: {{ .Values.node.name }}-sequencer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2
